---
title: Runtime Calibration as State-Trajectory Feedback Control in Quantum-Classical Workflows
title_zh: 量子-经典工作流中作为状态轨迹反馈控制的运行时校准
authors: Xiaolong Deng
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11860v1"
tags: ["query:sc-qubit-chip"]
score: 6.0
evidence: 将超导处理器运行时校准建模为量子门保真度的反馈控制
tldr: 在量子-经典变分工作流中，超导设备的校准质量会随时间漂移，而现有调度器将其视为静态。本文将此问题建模为有限时间预算下的状态轨迹反馈控制问题，研究当前校准投入能否改善未来优化轨迹。通过设计等效年龄状态和状态重置成本，用有限时域滚动控制器在不同延迟条件下评估反馈校准策略。结果表明，在局域毫秒级和紧耦合回路条件下，反馈校准能带来正增益，且增益随任务质量敏感度和初始校准年龄增大而增加。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11860v1/fig-001.webp\", \"caption\": \"Fig. 1. Physical mechanisms underlying the latency-regime comparison. Longer round-trip time penalizes runtime calibration in two distinct ways: it increases the duration of interactive multi-round primitives and reduces the realized recovery efficiency of feedback actions. The latter penalty is applied only to runtime recovery, not to offline scheduled calibration or to baseline circuit quality, which keeps the open-loop comparison fair.\", \"page\": 4, \"index\": 1, \"width\": 860, \"height\": 264, \"figure_number\": \"1\"}, {\"url\": \"assets/figures/arxiv/2605.11860v1/fig-002.webp\", \"caption\": \"Fig. 2. Representative time-domain trajectories at a0 = 12 h, α = 0.7, and Tclass = 1 s. Top: L2 freshness; Bottom: optimization gap g(t). Calibration acts as a discontinuous state reset. Runtime calibration is useful only when the state reset lowers the downstream gap trajectory enough to repay its wall-clock cost.\", \"page\": 6, \"index\": 2, \"width\": 860, \"height\": 429, \"figure_number\": \"2\"}, {\"url\": \"assets/figures/arxiv/2605.11860v1/fig-003.webp\", \"caption\": \"Fig. 3. Runtime gain ∆open relative to the strengthened open-loop reference family. Top: greedy (H = 1); Bottom: rollout (H = 6). Columns correspond to the cloud-like, local-ms, and tight-loop latency regimes. The axes scan algorithmic sensitivity α and initial calibration age a0, respectively. Red regions indicate that runtime control loses to the best open-loop policy; green regions indicate positive runtime gain. The black contour marks ∆open = 0 where visible. Local-ms feedback opens a finite positive-gain region, while tight-loop feedback gives the broadest operating margin in this scalar model.\", \"page\": 7, \"index\": 3, \"width\": 860, \"height\": 508, \"figure_number\": \"3\"}, {\"url\": \"assets/figures/arxiv/2605.11860v1/fig-004.webp\", \"caption\": \"Fig. 4. One-dimensional slices of trajectory-runtime gain. (a) Gain versus algorithmic sensitivity α at fixed initial age a0 = 12h. (b) Gain versus initial calibration age a0 at fixed sensitivity α = 0.7. Tight-loop feedback gives the largest positive margin, while cloud-like feedback becomes increasingly unfavorable as sensitivity or recoverable age increases.\", \"page\": 7, \"index\": 4, \"width\": 420, \"height\": 489, \"figure_number\": \"4\"}, {\"url\": \"assets/figures/arxiv/2605.11860v1/fig-005.webp\", \"caption\": \"Fig. 5. Action diagnostics for the trajectory runtime controller (H = 6). Top: Total runtime calibration actions executed within the fixed budget. Bottom: The fraction of those actions classified as heavy primitives. Cloud-like feedback can trigger many calibrations, but they are almost entirely light actions. Tight-loop feedback selects far fewer actions, but they are predominantly heavy state resets. Runtime-control quality is therefore determined by intervention value, not by invocation count alone.\", \"page\": 8, \"index\": 5, \"width\": 860, \"height\": 454, \"figure_number\": \"5\"}, {\"url\": \"assets/figures/arxiv/2605.11860v1/fig-006.webp\", \"caption\": \"Fig. 6. Classical-loop timescale sensitivity for trajectory runtime control. Top: fixed-iteration workload, where the wall-clock budget grows with Tclass. Bottom: fixed wall-clock budget, with Tbudget = 600 s. In the fixed-wallclock scan, runtime gain is non-monotonic because calibration must be spread over the remaining future iterations. Positive gain is controlled by the matching between calibration overhead, device drift, and algorithm-loop cadence.\", \"page\": 8, \"index\": 6, \"width\": 420, \"height\": 490, \"figure_number\": \"6\"}]"
motivation: 超导量子比特的校准保真度在小时尺度上漂移，现有的运行时调度却将后端质量当作静态处理，忽略了校准的时间维度。
method: 将运行时校准形式化为状态轨迹反馈控制问题，定义漂移的等效年龄状态和代价高昂的状态重置动作，并使用有限时域滚动控制器在固定时墙钟预算下评估策略。
result: 反馈校准在局域毫秒和4微秒的紧回路条件下可获得正增益，且增益随工作负载质量敏感度和初始校准年龄增加而扩大，而云延迟条件下反馈控制不具竞争力。
conclusion: 在低延迟且存在多校准目标处理压力时，紧耦合回路集成的反馈校准优势更为显著，而局域毫秒与紧回路控制在单目标恢复上的性能差距不大。
---

## 摘要
在运行变分工作负载的超导器件中，门和读出保真度会在小时尺度上漂移，而现有的运行时调度器将后端质量视为静态。校准的时间维度尚未解决。我们将运行时校准形式化为固定墙钟预算下的状态轨迹反馈控制问题，并研究现在花费时间进行校准能否改善未来的优化轨迹。校准质量代理被表示为漂移的等效年龄状态，恢复动作被建模为代价高昂的状态重置，并通过整个执行窗口上的时间积分优化差距来评估策略。使用有限视界滚动控制器，我们在三种延迟机制下比较了反馈校准与一系列增强的开环基线：类云环境（25毫秒）、本地毫秒级（1毫秒）和紧致环路（4微秒）。结果显示出一个明确的排序：类云环境下的反馈通常不具竞争力，而本地毫秒级和紧致环路则开启了一个正增益区域，该区域随工作负载的质量敏感度和初始校准年龄而增长。至关重要的是，对于单目标恢复，本地毫秒级和紧致环路控制之间的差距不大。紧致环路集成的优势在容量压力下显现，即当必须在同一控制窗口内处理多个校准目标时。

## Abstract
In superconducting devices running variational workloads, gate and readout fidelities drift on hour timescales, while existing runtime schedulers treat backend quality as static. The temporal dimension of calibration remains unresolved. We formulate runtime calibration as a state-trajectory feedback-control problem under a fixed wall-clock budget, and investigate whether spending time on calibration now can improve the future optimization trajectory. Calibration quality proxy is represented as a drifting equivalent-age state, recovery action is modeled as costly state reset, and policies are evaluated by time-integrated optimization gap over the full execution window. Using a finite-horizon rollout controller, we compare feedback calibration against a strengthened family of open-loop baselines across three latency regimes: cloud-like (25 ms), local-millisecond (1 ms), and tight-loop (4 $\mathrmμ$s). The results show a clear ordering: cloud-like feedback is generally uncompetitive, while local-ms and tight-loop regimes open a positive-gain region that grows with workload quality-sensitivity and initial calibration age. Crucially, the gap between local-ms and tight-loop control is modest for single-target recovery. The advantage of tight-loop integration emerges under capacity pressure, when many calibration targets must be processed within the same control window.